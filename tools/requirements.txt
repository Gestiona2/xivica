# Lo único que las herramientas de Python necesitan instalado además de Python.
# Solo lo usa excel_catalogo.py (y su prueba); validar.py y normalizar.py usan
# solo la biblioteca estándar.
#   pip install -r tools/requirements.txt
openpyxl>=3.1,<4
